<div class="hero">
  <p class="large brandon">Creator Arcade </br> is coming..</p>
  <p class="small brandon"><br>Learn Electronic Dance Music Alongside the World's Best DJ's.</p>
</div>

<div class="form-wrap clearfix <%= "ended" if Rails.application.config.ended %>">
  <% if Rails.application.config.ended %>
    <%= render "users/campaign_ended" %>
  <% else %>
    <%= render "users/campaign_ongoing" %>
  <% end %>
</div>
